version: 2
jobs:
  build:
    docker:
      — image: circleci/node:4.8.2
    steps:
      — checkout
      - run: 
        name: npm-install
        command: 'npm install'
      — run: 
        name: npm-test
        command: 'npm test'
